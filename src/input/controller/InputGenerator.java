package input.controller;

import java.awt.Toolkit;

public class InputGenerator {
	private final double WIDTH = Toolkit.getDefaultToolkit().getScreenSize().width / 2.0;
	private final double HEIGHT = Toolkit.getDefaultToolkit().getScreenSize().height / 2.0;
	private double xPos = WIDTH, yPos = HEIGHT;
}